node_modules
dist
.DS_Store
*.local

# 本地开发日志
*.log

# 本地 AI 工具配置，不属于项目源码；若要团队共享可自行去掉这两行
.codex/
__pycache__/
*.pyc

# 环境变量：默认值（不含密钥）要进版本库，只有本地覆盖才忽略。
# 注意 .env.* 会连 .env.development / .env.production 一起排除掉，
# 所以这里只能逐条排除本地覆盖文件。
.env
.env.local
.env.*.local
