# 开发环境（vite dev，端口 5173）
# 前端仍用相对路径 /api，由 vite.config.js 的 dev proxy 转发到 VITE_DEV_PROXY_TARGET，
# 这样开发时和线上走的是同一套相对路径，避免「本地能跑、上线 404」。
VITE_API_BASE_URL=/api
VITE_DEV_PROXY_TARGET=http://localhost:8080
