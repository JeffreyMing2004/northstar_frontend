# 生产环境（vite build）
# 站点：https://northstar.mingpixel.net
#
# 采用同源方案：前端请求 https://northstar.mingpixel.net/api/...
# OpenResty 再把 /api 反代到本机 127.0.0.1:8080 的 Spring Boot 后端。
# 好处是完全不产生跨域，后端 CORS 白名单只需保留该域名即可（已默认包含）。
#
# 仅当后端被部署到另一个域名时，才改成绝对地址，例如：
#   VITE_API_BASE_URL=https://api.mingpixel.net/api
# 并确认后端 app.cors.allowed-origins 已包含 https://northstar.mingpixel.net
VITE_API_BASE_URL=/api
